<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Trivial.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Trivial"
    Shell.FlyoutBehavior="Disabled"
    Title="Trivial">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

    <!--Recuerda tener esto o no navegara entre paginas aunque hayas creado metodos y botones-->

    <ShellContent Title="Capital" ContentTemplate="{DataTemplate local:capital}" Route="capital" />
    <ShellContent Title="País" ContentTemplate="{DataTemplate local:pais}" Route="pais" />

</Shell>
